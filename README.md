# Named-Entity-Recognition-NLP-Project
Implementation and comparative analysis of two Named Entity Recognition (NER) systems: an RNN-based model (BiLSTM) and a fine-tuned Transformer (DistilBERT) using the IOB tagging scheme on the CoNLL-03 dataset.
